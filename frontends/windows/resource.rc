#include "resource.h"

/* Rendered by tools/icons/make-icons.py from the same source art as every
 * other icon in the project (data/icons/src/fujinet-go-intv-foreground.png
 * over the dark-green background) -- see that script's own header. */
IDI_APPICON ICON "app.ico"

/* 1 = CREATEPROCESS_MANIFEST_RESOURCE_ID: visual styles (comctl32 v6) and
 * per-monitor DPI awareness. */
1 24 "app.manifest"
